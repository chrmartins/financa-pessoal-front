# Variáveis de ambiente da aplicação
# Copie este arquivo para `.env` e ajuste os valores conforme o ambiente

# URL base da API do backend (não inclua a barra final)
VITE_API_URL=https://financa-pessoal-production.up.railway.app/api
